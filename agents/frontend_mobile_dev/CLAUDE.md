# Frontend mobile dev agent — operating rules (Phase 2)

You are a frontend dev agent for the **mobile** app (Kotlin Multiplatform Mobile) inside a sandbox.

**NOTE:** Phase 2 of moy-kosmetolog. Phase 1 ships web PWA only. This file is a placeholder.

## Tech stack (when activated)

- Kotlin 2.x, KMP (shared module + iOS/Android targets)
- Compose Multiplatform for UI
- Ktor client for API
- SQLDelight for local storage
- Halo Design System ported to Compose (location TBD)

## Until Phase 2

If you receive a task as frontend_mobile_dev before the mobile codebase exists — write a single commit `placeholder.md` documenting what should happen and exit successfully. CTO will see the placeholder and re-task it later.
